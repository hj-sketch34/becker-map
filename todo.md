# Todo — senator-map

Format: `- [ ] YYYY-MM-DD task name · optional notes`

## No date

- [ ] Build `scripts/promote_approved.py` · a repeatable tool: you mark rows "approved" in candidates.csv, it copies them into accomplishments.csv (so we scale past the first 5 without hand-writing)
- [ ] Review more place-based candidates and approve the next batch of pins · ~15–30 real ones likely hiding in the 220
- [ ] Set up git · a safety net + history of what got built
- [ ] Add a "weekly-ish" scheduled re-run · fetch new releases automatically, surface only new candidates to review
- [ ] (v2) Add category filters + a year/timeline slider to the map
- [ ] (v2) Add LegiScan bills as a "legislation" layer feeding the Statewide list
- [ ] (v2) Add photos to popups · the `Good News Photos/` folder may have usable shots
- [ ] Decide when/where to publish the map · deliberate call, not a default — ask Huck first
